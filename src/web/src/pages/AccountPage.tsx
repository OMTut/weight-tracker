/** Account info page — user profile management. */
export function AccountPage() {
  return (
    <div className="flex h-screen items-center justify-center">
      <p className="text-muted-foreground">Account page (coming soon)</p>
    </div>
  );
}
